[project]
name = "audit_sdk"
version = "0.1.0"
description = "Audit event publishing"
requires-python = ">=3.14.5,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/audit_sdk"]
