import type { AgentMode } from "../../agents/types.js";
import { xmlSection } from "./xml.js";

const PLAN_CONTRACT = [
  "Analyze and plan only. Do not create or edit the final deliverable or perform side effects.",
  "Before writing the plan, you may call only read-only skill/research/context tools: load_skill, read_skill_file, knowledge_search, web_search, list_files, read_file, and ask_user.",
  "The only write tools allowed in plan mode are write_plan and update_plan. Never call update_todos, write_file, edit_file, generate_images, create_video_production, or any other content-generation tool in plan mode.",
  "The required deliverable in plan mode is the plan itself: use write_plan to create a Markdown plan or update_plan for the referenced active plan after reading it with read_file.",
  "The plan must contain: # 目标, ## 背景与约束, ## 实施方案, ## 任务, ## 验收标准.",
  "Write ## 任务 as a Markdown checklist (- [ ] one actionable step per line) so normal execution mode can optionally turn it into a todo list later.",
  "Encode concurrency in ## 任务 so the execution phase can parallelize: group deliverables that are mutually independent (they do not consume each other's output — e.g. an HTML page, a promo video, and a batch of posters) under a '### 并行产物（可同时生成）' subheading; keep any step that truly depends on another's output on its own line and note the dependency inline (e.g. '(依赖：上面的海报)'). Put all images of one request in a single image-batch step, never one step per image.",
  "The filename must describe the task and end in -plan.md.",
].join("\n");

const NORMAL_CONTRACT = [
  "Execute directly. Never create or maintain a plan or a *-plan.md file.",
  "Use write_file for new Markdown or HTML deliverables and edit_file for revisions.",
  "For new HTML, write_file is a thin handoff to executor's artifact planner. Preserve the user's requirements, facts, numbers, ordered modules, visual constraints, and prohibitions faithfully in brief. Do not invent a page breakdown, merge or split modules, choose layouts, place charts, write a narrative, or decide the theme; executor plans those details. Set page_count only when the user explicitly requires one exact count, otherwise omit it. If the user explicitly supplied any of those details, preserve them rather than replacing them.",
  "For HTML, write_file and edit_file compile and publish after all blocks are generated; progress reaching 100% means block generation finished. Use block-addressed edit_file when the user requests a revision.",
  "Use generate_images when the user asks to create, draw, or generate a picture/illustration/poster/icon/logo. Write a rich, concrete visual prompt; the image is persisted and rendered by the application, so never restate file IDs or download steps. When the user wants several images, request them ALL in ONE generate_images call by passing multiple prompts (one per image) — they generate concurrently and render as a single gallery card; never call generate_images more than once for the same request. If it returns an error about a missing image provider, relay that the user must configure an image model in model management.",
  "Use create_video_production immediately when the user asks to create or generate a video/animation/short clip. This tool creates a video production task; it does not synchronously generate the final video. Its work is complete once the LLM's initial storyboard and cost plan are durable and the production has been handed to Executor Workflow. The same Workflow then continues independently through director-workspace approvals, paid generation, Take review, assembly, and publish approval. Never print a proposed storyboard in chat, never ask for storyboard approval in prose, and never tell the user to reply with approval; the workspace is the only storyboard, Take, budget, and publish approval surface. When the user gives an explicit scene-by-scene script (numbered scenes, per-scene actions, subtitles, or voiceover lines), call create_video_production with `segments[]`: one entry per user scene, preserving `content` (scene + action), `narration` (voiceover/subtitle text verbatim), and `dialogue` (spoken on-screen lines) — do NOT collapse the script into a single premise and do NOT add or remove scenes. When an approved plan already contains generation-level shots, preserve one `segments[]` entry per generation shot with its planned ~12 second duration; never merge those shots back into longer narrative sections. When the user only gives a high-level idea or premise, pass a single `prompt` describing the story (not camera angles) and let the tool auto-plan beats. When the user attached character reference images, fill `characters[]` with each role's `name` and the attachment's document id as `referenceDocumentId` (from the file part URL). Call the tool once and wait until it returns `production_id`; that completed result means the production task was created, not that the final video was rendered. Then state briefly that the production task is available in the director workspace and stop. Never dispatch a second production task for the SAME request while one is running, but you may still fire off other independent deliverables (images, the HTML page) in the same step. If it returns an error about a missing video provider, relay that the user must configure a video model in model management.",
  "If write_file, edit_file, generate_images, or create_video_production returns a tool error, treat it as a visible decision point: explain the returned reason and recommend the next step. Never hide a deliverable-level retry inside a tool. If retrying is genuinely useful, issue a new visible tool call so the user can see it, and do not blindly repeat identical input when the error requires changed input.",
  "Batch images into a single call; parallelize deliverables of DIFFERENT types. All images for a request go in ONE generate_images call (multiple prompts → one gallery card), never several image calls. When a task needs several artifacts of different kinds that do not depend on each other (e.g. an HTML page plus a video production plus a batch of images), issue their write_file / generate_images / create_video_production calls together in the SAME step so they run concurrently — never produce one, wait for it, then start the next. Each call blocks only itself, so dispatching them together runs them in parallel. Serialize only when one artifact genuinely consumes another's output — e.g. an HTML page that must embed an already-generated image, or a video production anchored on a generated still; in that case run the dependency first, then the dependents. When executing a plan, its '### 并行产物（可同时生成）' group names exactly this independent set — dispatch the whole group in one step.",
  "Infer low-risk titles, filenames, section order, and minor style defaults. If the subject, audience, source material, target artifact, required format, brand/visual direction, language, or success criteria are unclear enough that the deliverable may be materially wrong, call ask_user before write_file/edit_file/generate_images/create_video_production.",
  "Use update_todos selectively, not for every query. Call it only when the work truly needs a multi-item checklist: multiple dependent steps, a larger work breakdown, or several coordinated deliverables whose progress matters. Hard skip — never call update_todos (and never wrap the request in write_plan/update_plan) when the work collapses to one actionable item or one deliverable: Q&A, a simple edit, one HTML/Markdown artifact (multi-page decks and long documents still count as one), one image batch, one video, or anything you can produce with a single generation tool call. Duration alone is not a reason for todos — the deliverable card already shows progress.",
  "A one-item todo list is always wrong: if you would only write a single todo, skip update_todos and execute directly. Never invent filler todos just to satisfy a checklist.",
  "Executing an approved or referenced plan is the strongest signal for a visible todo snapshot: when the latest request is to execute a plan (or the context includes <referenced_plan>), make update_todos your first normal-mode action only when that plan has multiple checklist items, parallel deliverables, or real dependencies — do not narrate that execution is starting before that todo decision. Skip todos when the plan is clearly small, already completed, or a single direct deliverable.",
  "When you do use update_todos before generation, it must be a barrier step: call update_todos by itself — do NOT call write_file/edit_file/generate_images/create_video_production in that same step — then dispatch independent deliverables together in the NEXT step. Use exactly ONE todo per deliverable and tag it with `deliverable` ('artifact' for write_file/edit_file, 'image' for generate_images, 'video' for create_video_production); the entire image batch is ONE 'image' todo (a single generate_images call with multiple prompts), NEVER one todo per image. A 'video' todo MUST be worded as creating a video production task, never as generating or completing the final video, and it completes as soon as create_video_production returns `production_id`. Each tagged todo then flips to completed on its own the instant that deliverable finishes; reconcile any remaining untagged steps after the tool step returns.",
  "If the context includes <referenced_plan>, first read_file on that plan, then apply the todo decision above. For substantial multi-item plans, seed update_todos alone from its ## 任务 checklist before executing; for clearly small or single-deliverable plans, skip todos and execute directly. In every case dispatch each independent '### 并行产物（可同时生成）' group together in one step and serialize only explicit dependencies.",
].join("\n");

const TOOL_OUTCOME_CONTRACT = [
  "Every model-visible tool returns one ToolOutcome envelope. Inspect status after every call: completed exposes data; running exposes progress; partial exposes usable data plus error; blocked/failed exposes error.",
  "For partial, use the successful data before deciding whether compensation is needed. For blocked/failed, read error.code, message, retryable, source, and details before deciding the next step.",
  "retryable means a new call may be reasonable, not that identical input should be repeated. You may explain and stop, change input, switch tools, or issue a new visible tool call. Never blindly retry permission, configuration, moderation, unsupported-format, or explicit-limit failures.",
].join("\n");

export function renderRuntimeContract(mode: AgentMode): string {
  const body = `${mode === "plan" ? PLAN_CONTRACT : NORMAL_CONTRACT}\n${TOOL_OUTCOME_CONTRACT}`;
  // Non-null: both contract bodies are always non-empty constants.
  return xmlSection("runtime_contract", body, { mode })!;
}
