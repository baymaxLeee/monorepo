packages:
  - "apps/*"
  - "packages/*"

# esbuild needs its platform-binary postinstall; MSW's notice script is optional.
onlyBuiltDependencies:
  - esbuild
ignoredBuiltDependencies:
  - msw
allowBuilds:
  esbuild: false
  msw: false
