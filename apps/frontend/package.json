{
  "name": "frontend-workspace",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "pnpm --dir ../.. lint:ts:frontend",
    "format": "pnpm --dir ../.. format:ts:frontend",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@project/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "tailwindcss": "catalog:",
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": "24.18.0"
  },
  "packageManager": "pnpm@11.9.0"
}
