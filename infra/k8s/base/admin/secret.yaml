# Placeholder Secret — NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: admin-secrets
type: Opaque
stringData:
  POSTGRES_HOST: "REPLACE_ME"
  POSTGRES_USER: "admin"
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_HOST: "REPLACE_ME"
  # Fernet key encrypting model_providers.api_key_enc at rest.
  # Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
  ADMIN_SECRET_KEY: "REPLACE_ME"
  INTERNAL_API_TOKEN: "REPLACE_ADMIN_TOKEN"
  # Per-caller credentials accepted by admin's `/internal/*` API.
  INTERNAL_SERVICE_TOKENS: '{"chat":"REPLACE_CHAT_TOKEN","executor":"REPLACE_EXECUTOR_TOKEN","knowledge":"REPLACE_KNOWLEDGE_TOKEN","canvas":"REPLACE_CANVAS_TOKEN"}'
