# Placeholder Secret — NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: admin-secrets
type: Opaque
stringData:
  MYSQL_HOST: "REPLACE_ME"
  MYSQL_USER: "REPLACE_ME"
  MYSQL_PASSWORD: "REPLACE_ME"
  REDIS_HOST: "REPLACE_ME"
  # Fernet key encrypting model_providers.api_key_enc at rest.
  # Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
  ADMIN_SECRET_KEY: "REPLACE_ME"
  # Shared secret presented by chat (and any future LLM consumer) on
  # admin's `/internal/*` API. Must match chat-secrets.INTERNAL_API_TOKEN.
  INTERNAL_API_TOKEN: "REPLACE_ME"
