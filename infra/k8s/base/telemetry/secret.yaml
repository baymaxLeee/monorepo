# Placeholder Secret — NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: telemetry-secrets
type: Opaque
stringData:
  CLICKHOUSE_HOST: "REPLACE_ME"
  CLICKHOUSE_USER: "REPLACE_ME"
  CLICKHOUSE_PASSWORD: "REPLACE_ME"
