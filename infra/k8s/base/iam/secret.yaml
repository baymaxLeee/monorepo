# Placeholder Secret — NEVER commit real values to git.
# Keys consumed via envFrom in the iam Deployment:
#   - MYSQL_HOST, MYSQL_USER, MYSQL_PASSWORD
#   - ACCESS_TOKEN_SECRET     (must match gateway value)
#   - SUPER_ADMIN_PASSWORD    (only used when not production; safe to keep
#                              as a non-prod helper)
apiVersion: v1
kind: Secret
metadata:
  name: iam-secrets
type: Opaque
stringData:
  MYSQL_HOST: "REPLACE_ME"
  MYSQL_USER: "REPLACE_ME"
  MYSQL_PASSWORD: "REPLACE_ME"
  ACCESS_TOKEN_SECRET: "REPLACE_ME"
