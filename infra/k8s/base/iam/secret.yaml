# Placeholder Secret — NEVER commit real values to git.
# Keys consumed via envFrom in the iam Deployment:
#   - POSTGRES_HOST, POSTGRES_USER, POSTGRES_PASSWORD
#   - ACCESS_TOKEN_SECRET     (must match gateway value)
#   - SUPER_ADMIN_ACCOUNT, SUPER_ADMIN_EMAIL, SUPER_ADMIN_PASSWORD
#     (required system bootstrap credentials in production)
apiVersion: v1
kind: Secret
metadata:
  name: iam-secrets
type: Opaque
stringData:
  POSTGRES_HOST: "REPLACE_ME"
  POSTGRES_USER: "iam"
  POSTGRES_PASSWORD: "REPLACE_ME"
  ACCESS_TOKEN_SECRET: "REPLACE_ME"
