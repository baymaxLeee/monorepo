# Cookie attributes are critical for cross-origin SPA (frontend on Cloudflare,
# API on cluster):
#   REFRESH_COOKIE_SECURE=true
#   REFRESH_COOKIE_SAMESITE=none
#   REFRESH_COOKIE_DOMAIN=.your-domain.com  (set in overlay)
apiVersion: v1
kind: ConfigMap
metadata:
  name: iam-config
data:
  PORT: "8002"
  ENVIRONMENT: "development"
  POSTGRES_PORT: "5432"
  IAM_POSTGRES_DATABASE: "iam"
  ACCESS_TOKEN_TTL: "5m"
  REFRESH_TOKEN_TTL: "720h"
  REFRESH_COOKIE_NAME: "refresh_token"
  REFRESH_COOKIE_SECURE: "false"
  REFRESH_COOKIE_SAMESITE: "lax"
  GUEST_WORKSPACE_ID: "guest-org"
  GUEST_WORKSPACE_NAME: "游客工作空间"
  GUEST_WORKSPACE_SLUG: "guest-workspace"
  # Overlay sets REFRESH_COOKIE_DOMAIN to e.g. .your-domain.com for prod.
