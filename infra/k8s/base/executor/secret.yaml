# Placeholder Secret — NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: executor-secrets
type: Opaque
stringData:
  POSTGRES_HOST: "REPLACE_ME"
  POSTGRES_USER: "executor"
  POSTGRES_PASSWORD: "REPLACE_ME"
  # Full postgres:// connection string for the managed Workflow World
  # Postgres instance (execution/replay state — separate from POSTGRES_* above,
  # which is business truth). @workflow/world-postgres reads this directly.
  WORKFLOW_POSTGRES_URL: "REPLACE_ME"
  # Shared secret presented to admin/knowledge's `/internal/*` API and
  # checked on every inbound request to this service. Must match
  # chat-secrets.INTERNAL_API_TOKEN exactly.
  INTERNAL_API_TOKEN: "REPLACE_ME"
