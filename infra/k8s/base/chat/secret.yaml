# Placeholder Secret — NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: chat-secrets
type: Opaque
stringData:
  POSTGRES_HOST: "REPLACE_ME"
  POSTGRES_USER: "chat"
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_HOST: "REPLACE_ME"
  # Shared secret presented to admin's `/internal/*` API.
  # Must match admin-secrets.INTERNAL_API_TOKEN exactly.
  INTERNAL_API_TOKEN: "REPLACE_ME"
  # HMAC secret for tool-approval replay protection; must be identical on every chat replica.
  TOOL_APPROVAL_SECRET: "REPLACE_ME"
