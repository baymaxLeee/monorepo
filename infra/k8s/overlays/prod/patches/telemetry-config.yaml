apiVersion: v1
kind: ConfigMap
metadata:
  name: telemetry-config
data:
  ENVIRONMENT: "production"
  # Production sample rates — lower these once traffic is real.
  SAMPLE_RATE_PERFORM: "0.2"
  SAMPLE_RATE_EVENT: "1.0"
