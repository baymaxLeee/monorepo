# Strip placeholder Secrets from prod applies — real secrets are created
# out-of-band (see overlays/prod/README.md) and must not be overwritten by
# kustomize stringData placeholders from base/.
apiVersion: v1
kind: Secret
metadata:
  name: placeholder
$patch: delete
